apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`api.example.invalid`)
      kind: Rule
      services:
        - name: backend
          port: 8080
  tls:
    secretName: api-tls-placeholder
